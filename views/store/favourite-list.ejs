<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    table {
      width: 90%;
      margin: 40px auto;
      border-collapse: collapse;
      background: var(--card);
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
    }
    th, td {
      padding: 15px;
      text-align: left;
      border-bottom: 1px solid var(--border);
    }
    tr:nth-child(even) {
      background: var(--muted);
    }
    th {
      background: var(--primary);
      color: var(--primary-foreground);
    }
    .btn {
      padding: 6px 12px;
      border: none;
      border-radius: var(--radius);
      cursor: pointer;
      font-weight: bold;
      margin-right: 8px;
    }
    .btn-details {
      background: var(--secondary);
      color: var(--secondary-foreground);
      border: 1px solid var(--border);
    }
    .btn-details:hover {
      background: var(--muted);
    }
    .btn-remove {
      background: var(--destructive);
      color: var(--destructive-foreground);
    }
    .btn-remove:hover {
      background: var(--primary);
    }
  </style>
</head>
<body>
  <%- include('../partials/nav') %>

  <div class="container">
    <h1>❤️ My Favorite Homes</h1>

    <% if (favorites && favorites.length > 0) { %>
      <table>
        <thead>
          <tr>
            <th>Title</th>
            <th>Location</th>
            <th>Price</th>
            <th>Description</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% favorites.forEach(home => { %>
            <tr>
              <td><%= home.house_name %></td>
              <td><%= home.location %></td>
              <td>&#8377;<%= home.price %></td>
              <td><%= home.description %></td>
              <td>
                <a href="/details/<%= home.id %>" class="btn btn-details">Details</a>
                <form action="/removeFav" method="POST" style="display:inline;">
                  <input type="hidden" name="id" value="<%= home.id %>">
                  <button type="submit" class="btn btn-remove">Remove</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <p>You don’t have any favorites yet.</p>
    <% } %>
  </div>
</body>
</html>
